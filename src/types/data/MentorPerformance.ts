export type MentorPerformance = {
    averageMessagePerStudent? : number | null,
    averageRating? : number | null,
    daysActive? : number | null,
    id: number,
    name: string,
    reviewTimeInDays? : number | null,
    reviewTimeInMinutes? : number | null,
}